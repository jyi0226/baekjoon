#include <bits/stdc++.h>
using namespace std;

int n,stand[101010],light[101010],m,num;

int main()
{
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(nullptr);
    cin>>n>>m;
    for(int i=0;i<m;i++)
    {
        cin>>stand[i];
        light[stand[i]]=1;
    }

}
