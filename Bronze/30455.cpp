#include <iostream>
#include <vector>
#include <string>
using namespace std;

int t,d,n,s,p;
int main(){
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(nullptr);
	cin>>n;
	if(n%2!=0)cout<<"Goose";
	else cout<<"Duck";
}
